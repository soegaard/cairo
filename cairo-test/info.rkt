#lang info

(define collection "cairo-test")

(define deps '("base" "cairo"))

(define build-deps '())

(define pkg-desc "Test for the bindings for the 2d graphics library Cairo.")

(define pkg-authors '(soegaard))

(define version "1.0")

(define test-responsibles '((all jensaxel@soegaard.net)))
